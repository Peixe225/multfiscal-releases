# Código da aplicação: nunca servido diretamente, mesmo se o DocumentRoot
# apontar por engano para a pasta acima de public/.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
